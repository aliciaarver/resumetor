# Анализ задачи: Поднять Vitest и первый слой тестов

## 1. Что нужно сделать

Установить и настроить фреймворк тестирования **vitest** + инструменты для unit-тестов:
- Подключить `vitest` + `@vue/test-utils` + `jsdom`
- Создать директорию `tests/fixtures/resume-corpus/` с тремя PDF-файлами (примеры):
  - Single-column EN resume
  - Dense RU resume
  - Смешанный формат
- Написать unit-тесты для 5 модулей:
  - `src/entities/resume/model/normalize.ts`
  - `src/entities/resume/model/confidence.ts`
  - `src/utils/pagination.ts`
  - `src/utils/parseDiff.ts`
  - `src/utils/socialLinks.ts`
- Добавить `npm test` в `package.json`
- Интегрировать в CI/CD пайплайн

**Контекст:** согласно `docs/testing-strategy.md`, проект должен иметь многоуровневое тестовое покрытие (unit → contract → integration → e2e), но сейчас оно полностью отсутствует.

## 2. Комментарии в Trello

Комментариев нет.

## 3. Figma макет

Отсутствует (это техническая задача).

## 4. План действий

1. Изучить текущую структуру проекта и `docs/testing-strategy.md`
2. Установить зависимости (vitest, @vue/test-utils, jsdom)
3. Создать фикстуры и примеры PDF
4. Написать unit-тесты для указанных модулей
5. Добавить `test` скрипт в package.json
6. Проверить интеграцию с CI/CD

## 5. Область

Задача касается инфраструктуры тестирования всего приложения (логика обработки данных резюме, утилиты, парсинг). Затрагивает как логику (backend), так и потенциально фронт-утилиты.

**Специалист: fullstack**
