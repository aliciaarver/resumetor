# PM-Анализ: Решить судьбу `ModernResumePreview.vue`

## 1. Что нужно сделать

Принять **продуктовое решение** о судьбе компонента `ModernResumePreview.vue`:

**Вариант А (завершить фичу):**
- Интегрировать Modern шаблон в систему `RESUME_TEMPLATES`
- Добавить селектор выбора шаблонов в `BuilderView.vue`
- Убедиться, что шаблон корректно работает с пагинацией и разметкой `data-page-block`
- Протестировать вывод на печать/PDF

**Вариант Б (удалить мёртвый код):**
- Удалить компонент `src/components/preview/ModernResumePreview.vue`
- Удалить ключи переводов `builder.templateModern` из `stores/locale.ts`
- Очистить от незавершённой фичи

## 2. Текущее состояние

| Параметр | Значение |
|----------|----------|
| Компонент | `src/components/preview/ModernResumePreview.vue` (485 строк) |
| Статус | Не зарегистрирован в `RESUME_TEMPLATES` |
| Селектор | Отсутствует в `BuilderView.vue` |
| Переводы | Существуют (`builder.templateModern` в `locale.ts`) |
| Разметка | Имеет `data-page-block` для печати (готово) |

### Состояние кода

**resumeTemplates.ts:**
```
RESUME_TEMPLATES = [
  { id: 'classic', labelKey: 'builder.templateClassic', component: ResumePreview }
]
```
→ Modern шаблон отсутствует

**ModernResumePreview.vue:**
- Готовый компонент с полной разметкой
- Структура: заголовок + боковая панель с контактами, скиллами, языками
- Поддерживает персональные данные, опыт, образование, скиллы, языки
- Имеет корректные `data-page-block` для печати

**BuilderView.vue:**
- Нет селектора шаблонов для пользователя
- Нет упоминаний `templateModern`

## 3. Комментарии в Trello

Комментариев нет.

## 4. Figma

Нет ссылок на макеты в задаче или исходном коде.

## 5. Зачем пользователю

- **Если завершить:** пользователь сможет переключаться между шаблонами резюме и выбирать Modern дизайн
- **Если удалить:** код станет чище, нет путаницы с неработающей фичей

## 6. Ценность

- **Вариант А:** расширение функционала, выбор дизайна
- **Вариант Б:** чистота кодовой базы, отсутствие ненужного кода

## 7. Критерии готовности

**Вариант А (реализация):**
- [ ] Modern шаблон добавлен в `RESUME_TEMPLATES`
- [ ] Селектор шаблонов добавлен в UI `BuilderView.vue`
- [ ] Переключение между Classic и Modern работает
- [ ] Пагинация работает корректно для Modern шаблона
- [ ] Печать/PDF экспорт работает с `data-page-block`
- [ ] Все переводы на месте

**Вариант Б (удаление):**
- [ ] Компонент `ModernResumePreview.vue` удалён
- [ ] Ключи `builder.templateModern*` удалены из переводов
- [ ] Нет левых импортов/ссылок на Modern в коде
- [ ] Проверено, что ничего не сломалось

## 8. Область

**Frontend** — пользователь видит выбор шаблонов и визуальное отличие резюме в интерфейсе

---

Специалист: frontend
