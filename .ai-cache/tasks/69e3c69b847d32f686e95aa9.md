# Анализ задачи: [Tech Debt] Разбить монолит parseTextToResume.ts по стадиям pipeline

**ID карточки:** 69e3c69b847d32f686e95aa9  
**Дата анализа:** 2026-04-20

## 1. Что нужно сделать

Рефакторинг парсера резюме из файла:

- **Текущее состояние:** `src/utils/parseTextToResume.ts` содержит 1940 строк монолитного кода; типы утекают из `utils` в `composables/useResumeParser.ts`.
- **Целевое состояние:** 7 отдельных модулей в `src/features/upload-resume/lib/` для каждой стадии pipeline:
  - `pdf-extraction/`
  - `normalization/`
  - `profile-detection/`
  - `section-classification/`
  - `entity-extraction/`
  - и ещё 2
  
- **Что переместить:**
  - Создать `src/features/upload-resume/model/parse-resume-from-pdf.use-case.ts` (точка входа)
  - Переместить типы `confidence` в `src/entities/resume/model/confidence.ts`
  - Восстановить архитектурные границы, как описано в `docs/parser-pipeline.md` и `docs/adr/0001-architecture-boundaries-and-shared-pagination.md`

- **Обеспечение качества:** подключить contract-тесты между стадиями (см. `docs/testing-strategy.md`)

## 2. Комментарии

Комментариев нет. Задача полностью описана в карточке.

## 3. Figma

Макет отсутствует (это backend-задача).

## 4. План действий

1. Прочитать документы архитектуры (`docs/parser-pipeline.md`, ADR 0001)
2. Изучить текущую структуру `parseTextToResume.ts` и её использование
3. Определить границы 7 стадий pipeline
4. Создать модульную структуру в `src/features/upload-resume/lib/`
5. Переместить типы в правильные места
6. Реализовать contract-тесты
7. Проверить, что интеграция работает через `parse-resume-from-pdf.use-case.ts`

## 5. Область разработки

Это чистая **backend** задача:
- Рефакторинг внутренней архитектуры
- Перемещение типов и логики между модулями
- Нет видимых изменений для пользователя
- Нет UI компонентов
- Фокус на качестве кода, архитектуре и контрактах

Специалист: backend