# Tech Lead Review — 2026-04-19 (run 2)

- **Тип запуска:** повторный, через ~10 минут после `2026-04-18T17-04-06.md`.
- **Скоуп:** валидация: изменилось ли что-то в коде с прошлого анализа, актуальны ли находки, появился ли `TRELLO_TOKEN`.
- **Итог:** состояние проекта неизменно, код не менялся, дельты нет. Новых задач не завожу. Структурная проблема самого `TECH-LEAD.md` (вперемешку дока и анализ) описана ниже как наблюдение — правок сейчас не делал.

## 1. Что проверял

- `TECH-LEAD.md` — прочитан полностью.
- `.tech-lead-history/2026-04-18T17-04-06.md` — прочитан (оказался побайтно-идентичен `TECH-LEAD.md`; см. «Структурное наблюдение» ниже).
- `CLAUDE.md` — отсутствует в корне.
- Размеры ключевых файлов (команда `wc -l`):
  - `src/utils/parseTextToResume.ts` — **1940** (как в отчёте).
  - `src/stores/locale.ts` — **550** (как в отчёте).
  - `src/views/BuilderView.vue` — **515** (как в отчёте).
  - `src/components/preview/ResumePreview.vue` — **541**; `ModernResumePreview.vue` — **485** (как в отчёте).
  - `src/utils/pagination.ts` — 91; `parseDiff.ts` — 151; `socialLinks.ts` — 62; `pdfTextNormalizer.ts` — 483.
- `src/utils/socialLinks.ts` — прочитан целиком: уязвимость в `buildSocialLinkHref` **сохранилась**. Регексп `/^[a-z]+:/i` на строках 27/31 пропускает `javascript:` как «уже c протоколом», и значение уезжает в `<a :href>` превью.
- `src/utils/resumeTemplates.ts` — прочитан: в реестре по-прежнему только `classic`. `ModernResumePreview.vue` не зарегистрирован.
- Парсер профилей (`grep` по `parseTextToResume.ts`) — `ParserProfile = 'hh_ru' | 'en_cv' | 'generic'`. Рассинхрон с `docs/parser-pipeline.md` (там `single-column / two-column / dense-corporate / job-board-export`) сохранён.
- `package.json` — без изменений: нет ESLint / Prettier / Vitest в dev-deps. Скрипты: только `dev`, `build`, `preview`.
- `tests/`, `.eslintrc*`, `eslint.config.*`, `.prettierrc*`, `.github/` — отсутствуют.
- `scripts/` — пустой каталог (как и в прошлый раз).
- Переменные окружения: `TRELLO_KEY` и `TRELLO_BOARD_ID` присутствуют, `TRELLO_TOKEN` **не задан**.

## 2. Дельта относительно `2026-04-18T17-04-06.md`

Нет. Ни одного расхождения: код, конфиги, документация и состояние инструментария идентичны. Файлы в `src/` и `docs/` не переписывались (mtime видно по `ls -la`: последние правки 14 апр. и 28 марта).

Следовательно все находки прошлого отчёта (§5 A–E, §6) остаются актуальны как есть — здесь их не повторяю, чтобы не дублировать журнал.

## 3. Расхождения с задокументированными правилами

Новых за этот прогон не найдено. Прежние расхождения (таблица §6 `TECH-LEAD.md`) — в силе:

- `docs/parser-pipeline.md` декларирует другие профили, чем в коде.
- `README.md`, `docs/README.md`, `docs/adr/0001-*.md` содержат ссылки вида `/Users/alicia/Desktop/⛤/resumetor/…`.
- Остальные расхождения (монолит парсера, толщина `locale.ts`, отсутствие тестов/линта/CI) — это долг в коде, а не в документации.

## 4. Структурное наблюдение о `TECH-LEAD.md`

`TECH-LEAD.md` в текущем виде — **полный отчёт анализа**, а не «живая техническая документация». Он идентичен файлу `.tech-lead-history/2026-04-18T17-04-06.md` (тот же текст: резюме, тех-долг, задачи, рекомендованный порядок работ, ссылки на артефакты). По действующим правилам `TECH-LEAD.md` должен содержать только стек/архитектуру/соглашения/ключевые модули; находки и задачи — только в `.tech-lead-history/*` и трекере.

На этом прогоне я **не переписываю `TECH-LEAD.md`**, потому что:
- правила запрещают переписывать целиком, а для приведения к «чистой доке» потребовалось бы удалить §1, §5, §6, §7, §8, §9;
- содержимое §2, §3, §4 (стек, архитектура, ключевые модули) актуально и точно описывает код — этот сгусток и есть дока; удалять точно ничего не требуется;
- развилка «удалить ли разделы анализа из `TECH-LEAD.md`» — решение тех-лида, а не автоматической правки.

Рекомендация (для следующего прогона или для владельца):
- вынести из `TECH-LEAD.md` §1 «Резюме состояния», §5 «Тех-долг», §6 «Расхождения», §7 «Задачи», §8 «Порядок работ», §9 «Артефакты» — это снимок анализа, он уже лежит в `.tech-lead-history/2026-04-18T17-04-06.md`.
- оставить в `TECH-LEAD.md` §2 «Стек», §3 «Архитектура» (включая «Целевую структуру по ADR-0001» как контракт), §4 «Ключевые модули» — это и есть живая дока.
- после выноса добавить короткий раздел «Соглашения» со ссылками на `docs/adr/*`, `docs/parser-pipeline.md`, `docs/testing-strategy.md`.

## 5. Задачи в трекере

**Не заведено ни одной новой карточки.** Причины:
1. Нет новых находок — всё, что можно было бы завести, уже перечислено в §7 `TECH-LEAD.md` / `2026-04-18T17-04-06.md`.
2. `TRELLO_TOKEN` по-прежнему не задан, а `TRELLO_KEY` и `TRELLO_BOARD_ID` без токена не дают создать карточку (endpoint `/1/cards` требует `key+token`).

Что делать владельцу: пробросить `TRELLO_TOKEN` в окружение и либо запустить тех-лида ещё раз, либо создать карточки вручную одним проходом по списку в §7 `TECH-LEAD.md`. Формат готов — метка «Тех. лид» (purple), `idBoard = vG0cUkfL`, due для критической (7 дней) / высокой (14–30 дней).

## 6. Правки в файлах

Никаких правок в этот прогон:
- `TECH-LEAD.md` — не трогал (см. §4).
- Документы в `docs/` — не трогал (расхождение с профилями парсера — развилка лида, путевые ссылки — отдельный PR с правкой `./docs/...`, решение не автоматическое).
- Код — не трогал (задача лида — анализ, не правки).

## 7. Артефакты

- Отчёт за этот прогон: `.tech-lead-history/2026-04-18T17-15-33.md` (этот файл).
- Предыдущий отчёт: `.tech-lead-history/2026-04-18T17-04-06.md` (неизменён).
- Живая дока: `TECH-LEAD.md` (неизменён, требует последующего выноса разделов анализа — см. §4).
