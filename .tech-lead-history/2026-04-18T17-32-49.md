# Tech Lead Review — 2026-04-19 (run 3)

- **Тип запуска:** третий. Предыдущие: `2026-04-18T17-04-06.md` (полный анализ), `2026-04-18T17-15-33.md` (re-run, дельты нет, рекомендовано вычистить `TECH-LEAD.md` до «живой доки»).
- **Скоуп:** проверка состояния кода; `TECH-LEAD.md` на момент запуска пуст (0 байт) → пересоздан с нуля как чистая документация.
- **Trello:** не трогал. Это фаза анализа — задачи создаёт отдельный запуск по секции «Предлагаемые задачи» ниже.

## 1. Что проверял

- `TECH-LEAD.md` — **0 байт**. Перешёл в ветку «документации нет» и написал её с нуля (стек / архитектура / соглашения / известные отклонения). Файл в git не трекается (репозиторий вне `.git`), потому историю сравнивать не с чем — опирался на прошлые отчёты.
- `CLAUDE.md` — в корне отсутствует.
- `.tech-lead-history/` — прочитаны оба прошлых отчёта целиком.
- `package.json`, `tsconfig.app.json`, `vite.config.ts` — без изменений относительно прошлого прогона.
- Размеры ключевых файлов (`wc -l`):
  - `src/utils/parseTextToResume.ts` — **1940** (совпадает).
  - `src/stores/locale.ts` — **550** (совпадает).
  - `src/views/BuilderView.vue` — **515** (совпадает).
  - `src/components/preview/ResumePreview.vue` — **541**, `ModernResumePreview.vue` — **485** (совпадает).
  - `src/composables/useResumeParser.ts` — **357**, `usePdfExport.ts` — **168**, `pagination.ts` — **91**, `pdfTextNormalizer.ts` — **483**, `parseDiff.ts` — **151**, `socialLinks.ts` — **62**.
- `src/utils/socialLinks.ts` — прочитан целиком. XSS-вектор сохранён: `buildSocialLinkHref` на строках 27 и 31 использует `/^[a-z]+:/i` и пропускает `javascript:` как «уже с протоколом» прямо в `<a :href>` превью.
- `src/utils/resumeTemplates.ts` — прочитан. `RESUME_TEMPLATES` по-прежнему содержит только `classic`; `ModernResumePreview.vue` не зарегистрирован.
- `src/utils/parseTextToResume.ts` — grep по профилям: `type ParserProfile = 'hh_ru' | 'en_cv' | 'generic'` (строка 13), ветвления `detectParserProfile` на 219–238. Рассинхрон с `docs/parser-pipeline.md` сохранён.
- `tests/`, `.eslintrc*`, `eslint.config.*`, `.prettierrc*`, `.github/workflows/` — отсутствуют.
- `scripts/` — пустой каталог.

## 2. Дельта относительно `2026-04-18T17-15-33.md`

Изменение одно: `TECH-LEAD.md` был обнулён (в прошлом прогоне лежал полный отчёт, совпадающий с `.tech-lead-history/2026-04-18T17-04-06.md`). Это согласуется с рекомендацией run 2 («вынести анализ из `TECH-LEAD.md`, оставить только живую доку»). В этом прогоне я выполнил это явно — записал в `TECH-LEAD.md` чистую доку (стек, архитектура, поток данных, ключевые модули, соглашения, короткий маркерный раздел «Известные отклонения» без описания задач).

Код и документы в `src/` и `docs/` не менялись: mtime прежний (14 апр. для `src/`, 28 марта для `docs/`). Все находки run 1 в части кода остаются актуальны.

## 3. Расхождения с задокументированными правилами

Новых расхождений за прогон не найдено. Подтверждённые расхождения (из run 1, состояние не изменилось):

- **docs vs. код:** `docs/parser-pipeline.md` декларирует профили `single-column / two-column / dense-corporate / job-board-export`; в коде — `hh_ru / en_cv / generic`. Развилка решается тех-лидом: либо ADR на переименование профилей, либо правка документа.
- **ADR-0001 vs. код:** `parseTextToResume.ts` — монолит вместо 7 стадий. `BuilderView.vue` — page + widgets + features в одном файле. `utils/` смешивает shared/infra/feature-lib. `stores/locale.ts` — «толстый» стор со словарём и доменными мэпперами.
- **testing-strategy.md vs. код:** Vitest не подключён, `tests/fixtures/resume-corpus/` отсутствует.
- **README / docs vs. файловая система:** `README.md` и `docs/README.md` ссылаются на `/Users/alicia/Desktop/⛤/resumetor/…` — битый локальный путь (`xopa`, не `⛤`).
- **Безопасность:** `buildSocialLinkHref` без allow-list протоколов.

## 4. Правки в файлах за этот прогон

- `TECH-LEAD.md` — создан с нуля как чистая документация (был пуст).
- `.tech-lead-history/2026-04-18T17-32-49.md` — этот файл.
- Остальное — не трогал: код, `docs/`, `README.md`, `stores/`, `utils/`.

## 5. Предлагаемые задачи

Список совпадает с run 1 (состояние кода не менялось). Отдельная фаза «Сгенерировать задачи» пройдётся по этой секции и по текущим карточкам на доске, заведёт только отсутствующие. Заголовки стабильны, чтобы дедупликация по имени работала.

---

### [Tech Debt] XSS через протокол в URL соц-ссылок

- Что нарушено: `src/utils/socialLinks.ts:buildSocialLinkHref` на строках 27 и 31 пропускает любую схему, удовлетворяющую `/^[a-z]+:/i`. Значение попадает в `<a :href>` внутри `src/components/preview/ResumePreview.vue` и `ModernResumePreview.vue`. Пользователь может ввести `javascript:alert(1)` в поле URL и получить исполнение при клике по ссылке в превью/экспорте.
- Как исправить: ввести allow-list разрешённых протоколов (`http:`, `https:`, `mailto:`, `tel:`). Для любой другой схемы — возвращать пустую строку или принудительно префиксовать `https://`. Добавить unit-тесты на вредоносные входы (`javascript:`, `data:`, `vbscript:`, `file:`). Проверить также, что `getSocialLinkDisplayText` не рендерит вредоносный HTML (он рендерится как текст, но аудит нужен).
- Срочность: критическая.

### [Tech Debt] Разбить монолит `parseTextToResume.ts` по стадиям pipeline

- Что нарушено: `docs/parser-pipeline.md` и `docs/adr/0001-architecture-boundaries-and-shared-pagination.md` требуют 7 стадий парсера в отдельных модулях с явными контрактами. Сейчас 1940 строк в одном `src/utils/parseTextToResume.ts`; типы `ResumeBlockKey` и `ParseBlockMetrics` утекают из `utils` в `composables/useResumeParser.ts`.
- Как исправить: создать каталоги `src/features/upload-resume/lib/{pdf-extraction,normalization,profile-detection,section-classification,entity-extraction}/` и `src/features/upload-resume/model/parse-resume-from-pdf.use-case.ts`. Типы confidence перенести в `src/entities/resume/model/confidence.ts`. После разбивки подключить contract-тесты между стадиями, как описано в `docs/testing-strategy.md`.
- Срочность: высокая.

### [Tech Debt] Поднять Vitest и первый слой тестов

- Что нарушено: `docs/testing-strategy.md` предписывает unit / contract / integration / e2e с fixture-корпусом `tests/fixtures/resume-corpus/`. В проекте нет ни тестового рантайма, ни фикстур, ни скрипта `npm test`.
- Как исправить: подключить `vitest` + `@vue/test-utils` + `jsdom`; завести каталог `tests/fixtures/resume-corpus/` с минимум тремя PDF (single-column EN, dense RU, смешанный); покрыть unit-тестами `src/entities/resume/model/normalize.ts`, `confidence.ts`, `src/utils/pagination.ts`, `parseDiff.ts`, `socialLinks.ts`. Добавить скрипт `test` в `package.json` и включить его в пайплайн сборки.
- Срочность: высокая.

### [Tech Debt] Разнести i18n-словарь из `stores/locale.ts`

- Что нарушено: ADR-0001, раздел «Follow-up»: сторы должны быть тонкими state-контейнерами. Сейчас `src/stores/locale.ts` — 550 строк, содержит полный словарь Ru/En, `PROFICIENCY_ORDER`, мэппинг лейблов соц-ссылок, side-эффект на `document.documentElement.lang`.
- Как исправить: вынести сообщения в `src/shared/i18n/{ru,en}.ts` (или per-feature файлы: `features/upload-resume/i18n/*`, `entities/resume/i18n/*`). В сторе оставить только `locale`, `t`, `setLocale`, сайд-эффект синхронизации `<html lang>`. Доменные мэпперы (`socialLinkLabel`, `proficiencyLabel`) — в `entities/resume/lib`.
- Срочность: высокая.

### [Tech Debt] Подключить ESLint + Prettier + минимальный CI

- Что нарушено: нет статической защиты стиля и нет машинной проверки перед мерджем. Push в основную ветку ничем не защищён; strict TS без lint-правил `vue/*` и `import/*` пропускает типичные Vue-ошибки.
- Как исправить: добавить `eslint-plugin-vue` + `@typescript-eslint` + `eslint-plugin-import`, Prettier с поддержкой SCSS. GitHub Actions workflow `install → lint → vue-tsc → build` (и `test`, когда появится). Precommit через `simple-git-hooks` или `lefthook` (lint + typecheck на staged).
- Срочность: высокая.

### [Tech Debt] Персист черновика резюме в `localStorage`

- Что нарушено: `src/stores/resume.ts` держит данные в памяти. Рефреш страницы / случайное закрытие вкладки теряет все правки пользователя. Паттерн `useLocalStorage` уже используется в `stores/locale.ts`, т.е. зависимость и привычка есть.
- Как исправить: обернуть `data` в `useLocalStorage('resumetor:resume:v1', normalizeResumeData(createDemoResume()))` с дебаунсом записи (`useDebounceFn`). Продумать миграцию схемы (версионный ключ `v1` / `v2`) и то, как отличить «пользовательский черновик» от «демо-данных»: не персистить, пока пользователь не внёс первое изменение.
- Срочность: средняя.

### [Tech Debt] Решить судьбу `ModernResumePreview.vue`

- Что нарушено: `src/components/preview/ModernResumePreview.vue` (485 строк) не зарегистрирован в `src/utils/resumeTemplates.ts`; селектора шаблонов в `BuilderView.vue` нет. Переводы `builder.templateModern` лежат в `stores/locale.ts`. Либо мёртвый код, либо незавершённая фича.
- Как исправить: продуктовое решение. Если фича «переключение шаблонов» нужна — добавить в `RESUME_TEMPLATES`, вывести селектор в `BuilderView.vue`, покрыть разметкой `data-page-block`, проверить пагинацию. Если не нужна — удалить компонент и ключи `templateModern` из словаря.
- Срочность: средняя.

### [Tech Debt] Экспорт PDF как текст, а не растр

- Что нарушено: `src/composables/usePdfExport.ts` клонирует DOM и растеризует через `html2canvas` → `jsPDF.addImage`. Итог — картинка. UI (`pdf.hint` в `stores/locale.ts`) обещает пользователю, что метаданные «помогают ATS находить резюме». ATS без текстового слоя читает только Title/Subject/Keywords/Author — большая часть контента резюме невидима.
- Как исправить: либо переход на `jsPDF.html()` с учётом пагинации `measurePagedLayout`, либо `pdf-lib` + собственный текстовый рендерер из доменной модели `ResumeData`. Альтернатива — переформулировать UI-копирайт в `pdf.hint` так, чтобы не обещать ATS-читаемость. Решение продуктовое, но тянет за собой заметный технический блок.
- Срочность: средняя.

### [Tech Debt] Декомпозировать `BuilderView.vue`

- Что нарушено: ADR-0001 разводит слои (pages / widgets / features). `src/views/BuilderView.vue` — 515 строк: preview-скейлинг, табы форм, смена локали, экспорт, performance-notice. Это pages + widgets + features в одной вью.
- Как исправить: выделить `src/widgets/resume-form/`, `src/widgets/resume-preview/`, `src/features/export-pdf/`, `src/features/switch-locale/`. `pages/builder` оставить только композицией этих слоёв и страничными метаданными.
- Срочность: средняя.

### [Tech Debt] Устранить `any` в pdf-аннотациях и унифицировать `detectLinkLabel`

- Что нарушено: `src/composables/useResumeParser.ts` использует `any` для `pdf` и `annotation` из `pdfjs-dist`. Этот путь обрабатывает внешние файлы, поэтому потеря типов здесь особенно чувствительна. Функция `detectLinkLabel` в этом же файле дублирует логику `normalizeSocialLinkLabel` из `src/utils/socialLinks.ts`.
- Как исправить: подключить типы `pdfjs-dist` (`PDFDocumentProxy`, `AnnotationData`) и убрать `any`. `detectLinkLabel` либо переиспользовать `normalizeSocialLinkLabel`, либо переселить в общий `src/shared/lib/url.ts`.
- Срочность: средняя.

### [Tech Debt] Починить ссылки в `README.md` и `docs/**`

- Что нарушено: `README.md` и `docs/README.md` ссылаются на `/Users/alicia/Desktop/⛤/resumetor/…` — абсолютный локальный путь, где каталог `⛤` даже не совпадает с реальным `xopa`. Это не рабочие ссылки ни в гит-хостинге, ни на текущей машине.
- Как исправить: заменить все такие пути на относительные (`./docs/README.md`, `./docs/adr/0001-…`, `./docs/parser-pipeline.md`, `./docs/testing-strategy.md`). Пройтись grep по `⛤` во всех `*.md` в `docs/`.
- Срочность: низкая.

### [Tech Debt] Привести профили парсера в соответствие с документацией

- Что нарушено: `docs/parser-pipeline.md` (стадия «Profile detection») декларирует профили `single-column` / `two-column` / `dense-corporate` / `job-board-export`. В коде (`src/utils/parseTextToResume.ts:13`) профили называются `hh_ru` / `en_cv` / `generic`. Разные источники правды.
- Как исправить: решение тех-лида. Либо ADR на переименование профилей в коде и миграция (с сохранением эвристик, но с новой таксономией, основанной на раскладке PDF), либо редакция `docs/parser-pipeline.md` под текущее поведение (профили определяются по локали/источнику, а не по раскладке). Выбор определяет направление будущей разбивки парсера.
- Срочность: низкая.

### [Tech Debt] Рассмотреть замену `html2canvas` на `html2canvas-pro`

- Что нарушено: `html2canvas` 1.4.1 не поддерживает `oklch`, `color-mix`, часть современных gradient-функций. Пока в шаблонах таких цветов нет — проблем нет; но при расширении темизации или включении `ModernResumePreview.vue` уткнёмся.
- Как исправить: оценить `html2canvas-pro` (fork с поддержкой современного CSS) или альтернативы. Отметить в roadmap на случай расширения шаблонов/темизации — не срочно до появления второго шаблона.
- Срочность: низкая.

## 6. Артефакты

- Этот отчёт: `.tech-lead-history/2026-04-18T17-32-49.md`.
- Предыдущие отчёты: `.tech-lead-history/2026-04-18T17-04-06.md`, `.tech-lead-history/2026-04-18T17-15-33.md` (не изменены).
- Живая дока: `TECH-LEAD.md` — переписана с нуля как «чистая» документация без секций анализа.
